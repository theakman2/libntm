INCLUDE_DIRECTORIES(${CMAKE_SOURCE_DIR}/buildfiles)
INCLUDE_DIRECTORIES(${CMAKE_SOURCE_DIR}/ntm)

ADD_EXECUTABLE(ntmtest ${NTM_TEST_SOURCE_FILES})

TARGET_LINK_LIBRARIES(ntmtest ntm)

IF (UNIX) 
	TARGET_LINK_LIBRARIES(ntmtest m)
ENDIF()

ADD_TEST(ntmtestsuite ntmtest)