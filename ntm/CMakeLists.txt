SET(CMAKE_BUILD_TYPE Release)

ADD_LIBRARY(ntm ${NTM_SOURCE_FILES})

IF (UNIX) 
	TARGET_LINK_LIBRARIES(ntm m)
ENDIF()

INSTALL(
	TARGETS ntm
	ARCHIVE DESTINATION lib
	LIBRARY DESTINATION lib
	RUNTIME DESTINATION bin
)

INSTALL(FILES ${NTM_HEADER_FILES} DESTINATION include/ntm)